<% layout('/layout') -%>
<!-- todo: remove handle message here in the layout -->
<div class="container-fluid">
  <% [].concat(errors).forEach(function (err) { %>
    <div><%= err %></div>
  <% }) %>
</div>
